{
  "name": "my-docker-app",
  "version": "1.0.0",
  "description": "Basic App for Jenkins Pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
